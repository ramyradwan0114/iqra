<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- خدمة الأذان.
             من أندرويد ١٤، أي خدمة أمامية لازم يبقى ليها نوع معلن
             هنا وفي الكود، والاتنين لازم يتطابقوا وإلا التطبيق بيقع
             وقت التشغيل. النوع mediaPlayback لأن اللي بنعمله حرفيًا
             هو تشغيل ملف صوت.
             exported=false: محدش برّه التطبيق محتاج يناديها.
             ملاحظة: ممنوع حرفين شَرطة ورا بعض جوّه تعليقات XML. -->
        <service
            android:name=".AthanService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- مستقبِل المنبّه. exported=false لأن اللي بينادي عليه هو
             نظام المنبّهات نفسه بـ PendingIntent صريح، مش تطبيق تاني.
             وبيستقبل كمان إعادة تشغيل الموبايل وتحديث التطبيق عشان
             يرجّع المنبّهات (بتتمسح في الحالتين). -->
        <receiver
            android:name=".AthanReceiver"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- خدمة الأذان الأمامية. الإذن التاني مخصوص بنوع mediaPlayback
         ومطلوب من أندرويد ١٤ — من غيره التطبيق بيقع وقت ما يحاول
         يشغّل الخدمة. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- الأذان بياخد دقايق والشاشة مقفولة — القفل ده بيمنع المعالج
         من النوم وسط التشغيل فيتقطع الصوت. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
</manifest>
